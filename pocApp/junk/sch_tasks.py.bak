#!/usr/bin/env python
from celery import Celery
from libnmap.process import NmapProcess
import xml.dom.minidom
import sys
import logging
from datetime import timedelta
import datetime
from celery.decorators import periodic_task
import interval
import sys


logger = logging.Logger('catch_all')

app = Celery('sch_tasks', broker='pyamqp://',backend='rpc://',)

@periodic_task(run_every=timedelta(seconds=interval.sec))
def MyHello():
    rc = "MyHello"
    print ("SchTask%s:" %(rc))
    return rc
